services:
  # 🐍 BACKEND: FastAPI + PostgreSQL (Supabase)
  - type: web
    name: senatecnologo-backend
    env: python
    buildCommand: "pip install -r requirements.txt"
    startCommand: "uvicorn main:app --host 0.0.0.0 --port $PORT"
    rootDir: backend
    envVars:
      - key: DATABASE_URL
        sync: false
      - key: SECRET_KEY
        sync: false

  # ⚛️ FRONTEND: React + Vite (Definido como Web Service con env: static)
  - type: web
    name: senatecnologo-frontend
    env: static
    buildCommand: "npm install && npm run build"
    staticPublishPath: dist
    rootDir: frontend
    envVars:
      - key: VITE_API_URL
        fromService:
          name: senatecnologo-backend
          type: web
          property: url
